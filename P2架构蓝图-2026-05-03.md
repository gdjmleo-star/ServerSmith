# ServerSmith P2 架构蓝图（13-OTC 签发）

**签发人**：穆凝雪
**时间**：2026-05-03 17:18 CST
**基于**：P1 V1.1 已完成 ✅ → P2

---

## 1. P2 目标

1. **成本展示板块**
   - 月度总支出（所有服务器月租汇总）
   - 各线路每GB成本（月租 / 总流量额度）
2. **TopEmby 跳转**
   - 首页导航增加 TopEmby 入口
3. **告警管理**
   - 离线告警阈值配置（连续离线多久触发告警）
   - 流量超额告警阈值（百分比阈值）
   - 告警列表展示 + 确认/已读功能
4. **告警通知**
   - 页头 Notification Badge
   - 未读告警数量红色角标

> ~历史趋势图已包含在 P1，P2 不重复开发~

---

## 2. 数据设计

### 2.1 成本展示 — 纯计算逻辑，不需要新表

后端在 `/api/dashboard` 响应中增加两个计算字段：

```json
{
  "total_monthly_rent": 1580.00,
  "cost_per_gb": [
    { "carrier": "4837", "total_quota_gb": 2000, "monthly_rent": 400, "cost_per_gb": 0.20 },
    { "carrier": "CN2_GIA", "total_quota_gb": 500, "monthly_rent": 600, "cost_per_gb": 1.20 }
  ]
}
```

计算方式：
- `total_monthly_rent` = SUM(所有服务器 monthly_rent)
- `cost_per_gb` = 按 carrier_type 分组，SUM(monthly_rent) / SUM(total_quota)

### 2.2 告警配置 — 新表 + 新字段

#### 新表：`alert_configs`

```sql
CREATE TABLE IF NOT EXISTS alert_configs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  server_id INTEGER NOT NULL REFERENCES servers(id) ON DELETE CASCADE,
  alert_type TEXT NOT NULL,           -- 'offline' | 'traffic_usage'
  enabled INTEGER DEFAULT 1,
  threshold TEXT NOT NULL,            -- '5' (分钟, for offline) | '90' (百分比, for traffic_usage)
  created_at DATETIME DEFAULT (datetime('now')),
  updated_at DATETIME DEFAULT (datetime('now'))
);
```

每个服务器每个 alert_type 只能有一条配置。

#### 已有 `alerts` 表增加字段

`alerts` 表（P0 已有）增加：

```sql
ALTER TABLE alerts ADD COLUMN acknowledged INTEGER DEFAULT 0;
ALTER TABLE alerts ADD COLUMN acknowledged_at DATETIME;
```

### 2.3 TopEmby 跳转

不需要后端改动。前端 `layout.tsx` 增加导航链接，使用环境变量配置 URL。

`web/src/lib/config.ts` 增加：
```ts
export const TOPEMBY_URL = process.env.NEXT_PUBLIC_TOPEMBY_URL || "http://localhost:3000";
```

---

## 3. 前端目录结构

```
web/src/app/
  layout.tsx                         ← 增加通知 Badge + TopEmby 导航

web/src/components/
  CostSummary.tsx                    ← 成本展示卡片（<200行）
  AlertConfigForm.tsx                ← 告警配置表单对话框（<200行）
  NotificationBadge.tsx              ← 页头通知角标（<50行）

web/src/hooks/
  useAlerts.ts                       ← 告警 CRUD + 已读标记
  useCostData.ts                     ← 成本数据拉取（实现在线计算）
```

---

## 4. 组件规格

### 4.1 CostSummary.tsx

展示位置：首页（`/`），在现有 StatCard 区域上方或下方。

展示内容：
- 一行："月度总支出 ¥1580.00"
- 按运营商维度表格："4837 — ¥400/月 — 2000GB — ¥0.20/GB"

### 4.2 NotificationBadge.tsx

展示位置：`layout.tsx` 顶部导航栏右侧。

行为：
- 轮询 `/api/alerts?acknowledged=false`，每 30 秒一次
- 未读数量 > 0 时显示红色 Badge
- 点击跳转到告警页面 `/alerts`

### 4.3 AlertConfigForm.tsx

对话框模式，在服务器编辑页或服务器详情页增加"告警配置"标签/按钮。

配置项：
- 离线告警：开启/关闭，阈值（分钟，默认 5）
- 流量告警：开启/关闭，阈值（百分比，默认 90）

### 4.4 告警列表页 `/alerts`

`web/src/app/alerts/page.tsx` — 告警列表，核心列：

| 列 | 内容 | 说明 |
|----|------|------|
| 服务器 | 名称 | 链接到编辑页 |
| 告警类型 | offline / traffic_usage | Badge |
| 消息 | "已离线 12 分钟" / "流量使用已超 92%" | 文本 |
| 时间 | 触发时间 | 北京时间 |
| 状态 | 未读/已读 | Badge |
| 操作 | 标记已读 | 按钮 |

---

## 5. 后端 API 变更

### 现有接口扩展

**`GET /api/dashboard`** — 增加：
- `total_monthly_rent: float`
- `cost_per_gb: []`

### 新建接口

**`GET /api/alert-configs?server_id=1`** — 获取服务器告警配置

**`PUT /api/alert-configs`** — 创建/更新告警配置
```json
{
  "server_id": 1,
  "alert_type": "offline",
  "enabled": true,
  "threshold": "5"
}
```

**`GET /api/alerts?acknowledged=false`** — 告警列表，支持筛选未读

**`PUT /api/alerts/:id/acknowledge`** — 标记已读

---

## 6. 全量文件清单（仅 P2 新增/修改）

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/api/dashboard.go` | 修改 | 增加成本计算逻辑 |
| `server/api/alert_configs.go` | 新建 | 告警配置 CRUD，不超 300 行 |
| `server/api/alerts.go` | 修改 | 加已读标记接口，不超 300 行 |
| `server/db/sqlite.go` | 修改 | 新增 alert_configs 表 + migration |
| `web/src/lib/config.ts` | 修改 | 增加 TOPEMBY_URL |
| `web/src/app/layout.tsx` | 修改 | 增加导航栏（TopEmby + NotificationBadge） |
| `web/src/app/alerts/page.tsx` | 新建 | 告警列表，<30 行 |
| `web/src/components/CostSummary.tsx` | 新建 | <200 行 |
| `web/src/components/AlertConfigForm.tsx` | 新建 | <200 行 |
| `web/src/components/NotificationBadge.tsx` | 新建 | <50 行 |
| `web/src/hooks/useAlerts.ts` | 新建 | <80 行 |
| `web/src/hooks/useCostData.ts` | 新建 | <40 行 |

---

## 7. P2 红线（扩展自 P1）

1. 成本数据不存表，只做在线计算（纯 SQL SUM/AVG）。
2. 告警配置为独立表，不塞入 servers 表或 server_plans 表。
3. NotificationBadge 的轮询间隔不低于 30 秒，不阻塞 UI。
4. TopEmby URL 走 config.ts 环境变量，不硬编码。
5. 告警列表页的"地图炮"已读（一键全部标记）在前端分批调用后端接口，不在后端开批量更新（防止误操作无法追溯）。
6. 所有新文件不超过红线尺寸。

---

## 8. P2 交付检查清单

| 项 | 验收标准 | 完成 |
|---|---------|------|
| 月度总支出 | 首页展示，数字精确到分 | ☐ |
| 每GB成本 | 按运营商分组展示 | ☐ |
| TopEmby 跳转 | 页头导航，配置走环境变量 | ☐ |
| 告警配置 | 服务器级离线/流量阈值，保存后生效 | ☐ |
| 告警列表 | 分类型展示、时间、未读状态 | ☐ |
| 标记已读 | 单条 + 批量 | ☐ |
| NotificationBadge | 未读计数 + 红色角标 | ☐ |
| 后端 migrate | alert_configs 自动建表 | ☐ |
| 文件红线 | 全部达标 | ☐ |

---

**架构版本**：V2.0
**签发人**：穆凝雪
**签发时间**：2026-05-03 17:18 CST

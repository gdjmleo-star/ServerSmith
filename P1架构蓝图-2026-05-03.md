# ServerSmith P1 架构蓝图（13-OTC 签发）

**签发人**：穆凝雪
**时间**：2026-05-03 16:06 CST — 补签，不应等到玲玲问才出图
**状态**：✅ 定稿，玲玲直接执行，不需再问

---

## 1. P1 目标

1. 服务器列表页（分页表格 + 统计摘要条）
2. 新增服务器（流量节点 / 项目服务器，共享 ServerForm）
3. 编辑服务器（复用 ServerForm）
4. 删除服务器（二次确认 + 级联删除）
5. 单台流量历史折线图

---

## 2. 前端目录结构（定稿）

```
web/src/app/
  servers/
    page.tsx                 ← 列表页壳（<20行，只渲染 ServerList + 摘要条）
    new/page.tsx             ← 新增页（<20行，只渲染 ServerForm mode="create"）
    [id]/edit/page.tsx       ← 编辑页（<20行，预填数据，渲染 ServerForm mode="edit"）
    [id]/history/page.tsx    ← 流量历史页（渲染 TrafficChart）

web/src/components/
  ServerList.tsx             ← 服务器表格（<300行）
  ServerForm.tsx             ← 新增/编辑表单（<300行）
  TrafficChart.tsx           ← recharts 折线图（<200行）

web/src/hooks/
  useServers.ts              ← 服务器列表 CRUD + 状态
  useHistory.ts              ← 单台流量历史拉取
```

**app/ 目录下的 page.tsx 必须控制在 20 行以内**——只负责导入组件和布局骨架，不准混业务逻辑。

---

## 3. 可变参数配置

`web/src/lib/config.ts`：

```ts
export const PAGE_SIZE = 20;
export const DEFAULT_HISTORY_DAYS = 30;
export const API_BASE = process.env.NEXT_PUBLIC_API_URL || "http://localhost:18080";
export const API_TIMEOUT_MS = 10000;
```

- 不准在任何 .tsx / .ts 文件里写死 20、30、10000。
- 流量历史的天数作为 URL query param（?days=7），走 DEFAULT_HISTORY_DAYS 兜底。

---

## 4. 异常处理契约

| 场景 | 行为 | 禁止 |
|------|------|------|
| API 不可达 | 红色 toast，文案"无法连接到服务器，请稍后重试" | 白屏、无反馈、控制台静默报错 |
| 删除服务器 | 二次确认弹窗："确认删除 {服务器名称}？此操作不可恢复。" | 无确认直接删除 |
| 表单空字段 | 前端字段级别红色提示："{字段名}不能为空" | 只等后端报 400、无前端校验 |
| 提交失败 | 红色 toast + 后端错误原文 | 只提示"失败"不说明原因 |

---

## 5. ServerForm 表单字段

### 模式 A：流量节点（plan_type='traffic'）

| 字段 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|
| name | 文本输入 | ✅ | — | 服务器名称 |
| host | 文本输入 | ✅ | — | IP 或域名 |
| ssh_port | 数字输入 | ❌ | 22 | SSH 端口 |
| ssh_user | 文本输入 | ❌ | root | SSH 登录用户 |
| carrier_type | 下拉选择 | ✅ | — | 4837/CN2_GIA/CN2_GT/CMI/CUII/软银/9929/其他 |
| monthly_rent | 数字输入 | ✅ | — | 月租（元） |
| total_quota | 数字输入 | ✅ | — | 月流量总额度（GB） |
| initial_used_gb | 数字输入 | ✅ | — | 期初已用流量（GB） |
| cycle_day | 数字输入 | ✅ | 1 | 计费复位日（1-31） |
| cycle_time | 时间输入 | ❌ | 02:00 | 复位时间 |

### 模式 B：项目服务器（plan_type='no_limit'）

| 字段 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|
| name | 文本输入 | ✅ | — | 服务器名称 |
| host | 文本输入 | ✅ | — | IP 或域名 |
| ssh_port | 数字输入 | ❌ | 22 | SSH 端口 |
| ssh_user | 文本输入 | ❌ | root | SSH 登录用户 |
| carrier_type | — | — | — | 不展示，节点专用字段 |
| monthly_rent | 数字输入 | ✅ | — | 月租（元） |
| expire_at | 日期选择 | ❌ | — | 到期日 |
| expire_notify_days | 数字输入 | ❌ | 7 | 提前几天提醒 |

**编辑模式**：预填已有数据，不展示 initial_used_gb（因为期初已用只在创建时有效）。

---

## 6. 后端最小改动

P0 后端已足够支撑 P1，只需要补充一个能力：

**编辑时检测 plan_type 变更**：如果服务器从 `no_limit` 改成 `traffic`，自动创建对应 `server_plans` 行。如果从 `traffic` 改成 `no_limit`，保留历史，不改 `server_plans`——只影响前端展示。

不准让用户先删账号再重建。

---

## 7. TrafficChart 设计

### 数据源

```
GET /api/servers/:id/history?days=7
```

返回格式（P0 已实现）：
```json
[
  { "date": "2026-05-01", "bytes_in": 1250000000, "bytes_out": 800000000 },
  ...
]
```

### 展示要求

- X 轴：日期。
- Y 轴：GB（后端返回 byte，前端 ÷ 1e9 显示）。
- 两条折线：入流量（青色）、出流量（橙色）。
- 图例可切换单线路。
- 空状态：显示"暂无历史数据"，不准报错或白屏。

### 使用库

recharts（已存在于依赖中）。

---

## 8. 表格列定义

| 列 | 内容 | 排序 | 说明 |
|----|------|------|------|
| 名称 | server name | 可 | 点击进入编辑页面 |
| IP | host | 可 | — |
| 类型 | node / project | 可 | Badge 展示 |
| 线路 | carrier_type | 可 | 仅节点显示 |
| 状态 | online/offline/unknown | 可 | 彩色 Badge |
| 已用流量 | used_gb / total_quota GB | 可 | 进度条 + 百分比 |
| CPU | agent 最新值 | — | 小号字体 |
| 内存 | agent 最新值 | — | 小号字体 |
| 月租 | monthly_rent | 可 | ¥ 前缀 |
| 到期 | expire_at | 可 | 超时红、7天内黄 |
| 操作 | 编辑 / 重启(带二次确认) / 删除 | — | 操作列，不排序 |

---

## 9. P1 执行红线

1. 单文件不超 300 行（page.tsx < 20 行）。
2. 禁止 Ant Design / Element Plus / 外部 CDN。
3. 禁止硬编码（PAGE_SIZE、DEFAULT_HISTORY_DAYS、API 地址全部走 config.ts）。
4. 禁止重复 hook 逻辑（useServers 统一管理服务器 CRUD 状态）。
5. 禁止白屏（所有 fetch 必须 try/catch + toast 反馈）。
6. 禁止无确认删除。
7. 禁止表单空字段直提。
8. 编辑模式不展示 initial_used_gb。

---

## 10. P1 交付检查清单

| 项 | 验收标准 | 完成 |
|----|---------|------|
| 列表页分页 | 展示前 20 条 | ☐ |
| 新增流量节点 | 表单完整，成功创建 | ☐ |
| 新增项目服务器 | 表单完整，成功创建 | ☐ |
| 编辑服务器 | 预填数据，保存后更新 | ☐ |
| 删除服务器 | 二次确认 → 级联删除 | ☐ |
| 新增时 plan_type 切换 | 表单字段联动 | ☐ |
| 流量历史图 | recharts 折线，空状态 | ☐ |
| 配置常量 | config.ts，无硬编码 | ☐ |
| 异常处理 | 红色 toast，无白屏 | ☐ |
| 前端 lint / build 通过 | 无错误无警告 | ☐ |

---

**架构版本**：V1.1
**签发人**：穆凝雪
**补签时间**：2026-05-03 16:06 CST

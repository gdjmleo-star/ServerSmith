FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
# Pre-copy soruce files as template (will be overridden by volume mount at runtime)
COPY . .
EXPOSE 3000
CMD ["npm", "run", "dev"]

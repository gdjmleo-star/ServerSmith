# ServerSmith 开发任务 — 给玲玲

## 项目定位

做一个轻量的服务器管理系统，帮老板管理 100+ 台服务器。不是大项目，就一个 Go 后端 + 前端页面 + 一个小探针程序。

---

## 核心功能一览

### 首页 — 分线路看板
打开就看到各线路剩余流量概况：
```
4837  剩余 12,200/20,000 GB   5台节点
CN2   剩余  3,200/ 5,000 GB   2台节点
CMI   剩余    800/10,000 GB   3台节点  ← 快用完
```
点进线路 → 看到该线路下每台服务器的剩余流量和状态。

### 服务器分两类

**节点服务器（流量节点）：**
- 有月流量额度（比如 2000GB/月）
- 有独立复位日（比如每月5号凌晨2点）
- 到复位日自动重启服务器、流量归零
- 显示已用/剩余流量

**项目服务器（业务部署）：**
- 没有流量额度，没有复位日
- 只看在线状态、健康数据和到期时间

### 核心逻辑：流量怎么算？
- 第一次添加节点时，手动输入「这个周期已经用了多少」（期初值）
- 之后探针每30秒上报网卡流量，系统自动算增量累加
- **重启不清零**，只有到复位时间才清零
- 任何时候打开页面看到的都是实时剩余

### 成本展示
- 每台服务器添加时输入月租（元）
- 首页底部显示：每月总支出、各线路每GB成本
- 不需要按天折算，不管复位日几号，统一按整月算

### 在线/离线监控
- 探针超过 5 分钟没上报 → 标记离线

### TopEmby 对接
- 不需要共用数据库
- 在 TopEmby 后台加一个跳转按钮，新标签页打开这个面板

---

## 技术选型

| 模块 | 技术 | 原因 |
|------|------|------|
| 探针 agent | Go 编译单二进制 | 扔到 100 台机器上零依赖 |
| 管理后端 | Go 单二进制 | 低内存低 CPU，一个文件搞定 |
| 数据库 | SQLite (WAL模式) | 100台每30秒上报，WAL扛得住 |
| 前端 | 内嵌在 Go 二进制里的页面 | 不依赖 CDN 或外部资源 |
| 部署（开发） | Docker Compose | 方便调试 |
| 部署（生产） | 单二进制 + systemd | 不需要装任何环境 |

---

## 数据表 6 张

1. **servers** — 服务器主表（名字、IP、类型、线路类型、月租、在线状态）
2. **server_plans** — 计费配置（额度、复位日、期初已用、到期日）
3. **traffic_snapshots** — 探针上报的实时快照（保留7天）
4. **traffic_history** — 按天汇总的历史数据（趋势图用）
5. **reboot_tasks** — 重启任务记录
6. **alerts** — 告警记录

详细字段定义见 PROJECT_MEMORY.md。

---

## API 清单

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | /api/report | 探针上报数据 |
| GET | /api/dashboard/by-carrier | 按线路汇总剩余流量（首页核心接口） |
| GET | /api/dashboard/costs | 成本总览 |
| GET | /api/servers | 服务器列表 |
| POST | /api/servers | 添加服务器 |
| GET | /api/servers/:id | 单台详情 |
| PUT | /api/servers/:id | 编辑 |
| DELETE | /api/servers/:id | 删除 |
| GET | /api/servers/:id/history | 历史趋势 |
| POST | /api/servers/:id/reboot | 手动重启 |
| GET | /api/dashboard | 总览（数量/在线/超限） |
| GET | /api/alerts | 告警列表 |

---

## 怎么交差
1. 先跑通探针上报 + SQLite 存储 + 首页看板展示
2. 再实现服务器管理 CRUD
3. 再实现周期复位 Cron + SSH 重启
4. 最后做成本展示、历史趋势、TopEmby 跳转按钮

**有问题直接问凝雪，或者问 Boss。**

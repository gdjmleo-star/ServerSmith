# ServerSmith P4 架构蓝图（生产就绪）

**签发人**：穆凝雪
**时间**：2026-05-03 18:00 CST
**基于**：P3 已验收 ✅

---

## 1. P4 目标

**一句话**：把 ServerSmith 从「Docker 里的开发项目」变成「下载即运行的二进制文件」。

具体四项：
1. **Go embed** — 内嵌前端静态文件，不再需要独立 Node 服务
2. **跨平台编译** — Makefile，一键编译 Linux ARM64 + Linux AMD64
3. **数据迁移** — P3 漏迁移的字段补全（`server_plans` 表 `expire_notify_days`）
4. **探针版本上报** — agent 端起头发送探针版本号

---

## 2. Go embed 内嵌前端

### 后端改动

`server/main.go` 增加：

```go
//go:embed web-out/*
var webFS embed.FS
```

`server/web/` 目录存放前端编译产物。

**`GET /` → 返回 web/index.html`**

### Docker Compose 调整

`docker-compose.yml`：
- 移除 `web` 服务
- 编译命令改为 `cd web && npm run build && cp -r out/* ../server/web-out/ && cd ../server && go build`
- 前端不再是独立服务，而是构建阶段产物

### 新文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/web-out/` | 新建 | git ignore，构建生成 |
| `server/web.go` | 新建 | 静态文件路由 |
| `Makefile` | 新建 | 一键构建 |

---

## 3. 跨平台编译

### Makefile

```makefile
.PHONY: build-all build-darwin build-linux-amd64 build-linux-arm64 clean

build-all: build-darwin build-linux-amd64 build-linux-arm64

build-darwin:
	cd web && npm run build
	mkdir -p ../server/web-out && cp -r out/* ../server/web-out/
	cd ../server && go build -o ../dist/serversmith-darwin .

build-linux-amd64:
	cd web && npm run build
	mkdir -p ../server/web-out && cp -r out/* ../server/web-out/
	cd ../server && GOOS=linux GOARCH=amd64 go build -o ../dist/serversmith-linux-amd64 .

build-linux-arm64:
	cd web && npm run build
	mkdir -p ../server/web-out && cp -r out/* ../server/web-out/
	cd ../server && GOOS=linux GOARCH=arm64 go build -o ../dist/serversmith-linux-arm64 .

clean:
	rm -rf dist/ server/web-out/ web/.next/
```

---

## 4. 数据迁移

### 补全 expire_notify_days

`server/db/sqlite.go` 增加 migration：

```go
_, _ = DB.Exec("ALTER TABLE server_plans ADD COLUMN expire_notify_days INTEGER DEFAULT 7")
_, _ = DB.Exec("ALTER TABLE servers ADD COLUMN agent_version TEXT DEFAULT ''")
```

### server_plans 表同步

P3 `service_plans` 表名可能需要统一。目前代码里使用的是 `plans` 还是 `service_plans`？审计后确认统一。

---

## 5. 探针版本上报

### Agent 端

`agent/main.go` 上报时，在 URL query 或 header 携带版本号：

```go
const AgentVersion = "1.0.0"
// URL: /api/report?server_id=X&version=1.0.0
```

### 后端

`server/api/report.go` 解析 `version` 参数，更新 `servers.agent_version`：

```go
version := r.URL.Query().Get("version")
if version != "" {
    _, _ = db.DB.Exec("UPDATE servers SET agent_version=? WHERE id=?", version, serverID)
}
```

---

## 6. P4 全量文件变更

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/web.go` | 新建 | Go embed 静态文件路由 |
| `server/web-out/` | 新建目录 | 前端构建产物 |
| `server/main.go` | 修改 | 注册嵌入路由 |
| `server/db/sqlite.go` | 修改 | 补 migration |
| `server/api/report.go` | 修改 | 接收探针版本 |
| `agent/main.go` | 修改 | 上报携带版本号 |
| `Makefile` | 新建 | 跨平台编译 |
| `docker-compose.yml` | 修改 | 移除 web 服务，改为构建阶段 |
| `.gitignore` | 修改 | 忽略 dist/ server/web-out/ web/.next/ |
| `README.md` | 新建 | 使用说明 |

---

## 7. P4 红线

1. Go embed 必须能正确加载静态文件，不允许 404。
2. Makefile 必须同时编译三种架构（darwin amd64, linux amd64, linux arm64）。
3. Docker Compose 只是本地调试工具，不参与生产构建。
4. 探针版本号为空时不阻塞上报流程。

---

## 8. P4 交付检查清单

| 项 | 验收标准 | 完成 |
|---|---------|------|
| Go embed | 单二进制运行，前端正常加载 | ☐ |
| Makefile | `make build-all` 生成三个二进制 | ☐ |
| 跨平台编译 | linux arm64 可以运行 | ☐ |
| expire_notify_days | migration 成功 | ☐ |
| agent_version | 上报日志可看到版本号 | ☐ |
| docker-compose | 移除 web 服务 | ☐ |

---

**架构版本**：V4.0
**签发人**：穆凝雪
**签发时间**：2026-05-03 18:00 CST

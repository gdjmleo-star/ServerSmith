# ServerSmith 给 14-Software（玲玲）的完整开发军令

**签发人**：13-OTC 穆凝雪
**项目**：ServerSmith
**签发时间**：2026-05-03 15:09 CST
**执行人**：14-Software 玲玲
**状态**：立即执行

---

## 0. 第一铁律：先读项目记忆库

玲玲，动手前先读取：

`/Users/omnileo/.openclaw/Data_Source/系统开发/ServerSmith/PROJECT_MEMORY.md`

里面是 ServerSmith 的唯一架构图纸。任何实现不得绕过它。没有读完，不准创建项目，不准写页面，不准先跑起来再说。

---

## 1. 项目目标

ServerSmith 是一个轻量流量额度管理与服务器运维面板，用来替代哪吒探针，管理 Boss 的 100+ 台服务器。

核心目标：

1. 按线路类型汇总剩余流量。
2. 每台节点独立计费周期，到点自动复位流量并重启。
3. 记录每台服务器月租，自动计算每 GB 成本。
4. 监控服务器健康状态、离线状态和续费到期日。

---

## 2. 技术栈定稿

### 2.1 后端

- Go
- SQLite
- SQLite 必须开启 WAL
- Go 管理端最终必须是单二进制
- 前端静态资源最终必须由 Go embed 内嵌

### 2.2 前端

强制使用：

- Next.js
- Tailwind CSS
- shadcn/ui

全面禁止：

- Ant Design
- Element Plus
- 老旧企业后台模板
- 外部 CDN 字体、图标、样式

UI 风格定稿：

- 深色运维驾驶舱
- 苹果系统般极简、现代、高级感
- 数据卡片清晰
- 状态颜色克制
- 危险状态一眼可见

---

## 3. 本地 Docker 开发环境

Docker 只作为本地开发沙盒，不是生产架构。

### 3.1 必须搭建

本地开发环境需要支持：

1. Go 管理端 API
2. SQLite 数据文件挂载
3. Next.js 前端开发服务
4. 本地模拟探针上报

### 3.2 禁止堆料

禁止引入：

- PostgreSQL
- Redis
- RabbitMQ
- Kafka
- Nginx
- Caddy
- 微服务拆分
- 复杂 Docker Compose 堆料

### 3.3 数据文件红线

SQLite 数据文件必须挂载到项目目录下，不能藏在容器临时层。容器删除后，数据库文件不能丢。

---

## 4. 当前宿主机端口现状

已经被占用的端口：

| 端口 | 占用方 | 处理 |
|------|--------|------|
| 3000 | TopEmby | 禁止占用 |
| 8080 | FileBrowser/Data_Source | 禁止占用 |
| 5432 | Emby Postgres | 禁止占用 |
| 6379 | Emby Redis | 禁止占用 |
| 11434 | Ollama | 禁止占用 |

ServerSmith 本地端口固定：

| 模块 | 端口 |
|------|------|
| Go 管理端 API | 18080 |
| Next.js 前端开发服务 | 13000 |
| SQLite | 不占端口 |
| 探针模拟上报 | 不单独暴露端口，直接上报 18080 |

严禁动 TopEmby、FileBrowser、Postgres、Redis、Ollama。

如果冲突，优先调整 ServerSmith，不能动现有服务。

---

## 5. 数据模型必须按记忆库执行

核心表：

1. servers
2. server_plans
3. traffic_snapshots
4. traffic_history
5. reboot_tasks
6. alerts

关键红线：

1. 流量节点与项目服务器必须区分。
2. carrier_type 只对流量节点有效。
3. 项目服务器不参与线路流量汇总。
4. used_bytes 初始化必须等于 initial_used_gb 转字节。
5. 探针只上报网卡总字节，不上报历史已用流量。
6. 管理端根据上一条快照计算流量增量。
7. used_bytes 只在 next_cycle_at 到达时清零。
8. 手动重启或意外重启，不能清零 used_bytes。

---

## 6. P0 开发顺序

第一阶段只做 P0，不准贪多。

### P0-1：项目骨架

创建 ServerSmith 项目骨架：

- Go 管理端目录
- Agent 探针目录
- Next.js 前端目录
- SQLite 数据目录
- 本地 Docker 开发配置

### P0-2：Go 管理端

实现：

1. API 服务监听 18080。
2. SQLite 初始化。
3. servers、server_plans、traffic_snapshots 等核心表建表。
4. 探针上报接口。
5. 服务器列表接口。
6. 仪表盘按线路汇总接口。

### P0-3：探针 Agent

实现：

1. 采集 CPU 使用率。
2. 采集内存使用率。
3. 采集磁盘使用率。
4. 采集网卡总入/总出字节。
5. 每 30 秒上报一次。
6. POST 失败重试 3 次，每次间隔 5 秒。
7. 本地不存任何历史数据。

### P0-4：流量累计逻辑

管理端收到探针上报后：

1. 更新服务器状态为 online。
2. 更新 last_report_at。
3. 查上一条 traffic_snapshots。
4. 计算 net_in/net_out 增量。
5. 增量累加到 server_plans.used_bytes。
6. 写入新快照。
7. 检查是否超限，必要时写 alerts。

### P0-5：前端仪表盘雏形

必须先做首页驾驶舱，不准先堆普通表格。

首页第一屏必须有：

1. 总服务器数。
2. 在线数/离线数。
3. 总月租成本。
4. 总剩余流量。
5. 即将到期、超限、离线告警。

线路卡片必须展示：

1. 线路类型。
2. 节点数量。
3. 总额度。
4. 已用流量。
5. 剩余流量。
6. 使用率进度条。
7. 每 GB 成本。
8. 异常节点数量。

---

## 7. P1 开发顺序

P0 通过 13-OTC 审查后，再做 P1。

P1 包括：

1. 服务器添加/编辑/删除。
2. 添加流量节点时必须输入：线路、月租、总额度、期初已用流量、复位日、复位时间。
3. 添加项目服务器时必须输入：名称、Host、月租、到期日。
4. 手动重启单台服务器。
5. 计费周期复位 Cron。
6. SSH 重启任务记录。

---

## 8. 重启能力红线

ServerSmith 可以控制服务器重启，但必须克制。

允许：

1. 单台手动重启。
2. 到计费周期自动重启。
3. 重启任务记录。

禁止：

1. 一开始就做复杂批量远程执行。
2. 做堡垒机式命令面板。
3. 页面一键无确认重启。

硬性要求：

1. SSH 重启必须有 10 秒超时。
2. 单台失败不能影响其他任务。
3. 手动重启按钮必须二次确认。
4. 重启不等于流量清零。

---

## 8.5 时间与时区红线

ServerSmith 的计费复位、到期提醒、离线判断、重启记录全部依赖时间。时区必须从第一天钉死。

唯一业务时区：Asia/Shanghai。

存储规则：

1. 所有事件时间统一 UTC 落库。
2. 前端统一按 Asia/Shanghai 展示。
3. Boss 输入的复位日和复位时间，按 Asia/Shanghai 解释，再转 UTC 存入 next_cycle_at。
4. 到期日 expire_at 是 Asia/Shanghai 业务日期，不能被浏览器或 UTC 解析搞成错一天。
5. 离线判断必须用 UTC 时间差，不准字符串比较。

禁止：

1. 不准混存北京时间、UTC、无时区字符串。
2. 不准依赖服务器系统时区。
3. 不准依赖浏览器本地时区。
4. 不准前端直接把模糊 Date 对象当业务时间提交。

交付时必须说明：

1. SQLite 时间字段是否统一 UTC。
2. 前端是否统一 Asia/Shanghai 展示。
3. next_cycle_at 如何由 cycle_day + cycle_time 计算。
4. expire_at 如何避免错一天。
5. 离线判断是否基于 UTC 时间差。

---

## 9. 离线判断红线

探针不上报不等于立刻离线。

规则：

- 超过 5 分钟没有上报，才标记 offline。
- 探针恢复上报后，状态改回 online。
- 离线事件写入 alerts。

---

## 10. 成本统计红线

Boss 只要月度固定成本，不要按天折算。

规则：

1. 全局每月总支出 = 所有服务器 monthly_rent 之和。
2. 某线路每 GB 成本 = 该线路节点月租总和 ÷ 该线路总流量额度。
3. 不按复位日折算。
4. 不追踪月内成本波动。

---

## 11. UI 红线

页面必须是深色运维驾驶舱。

禁止：

1. 彩虹状态色。
2. 大面积廉价渐变。
3. 巨型 CRUD 表格首页。
4. 企业 OA 风格。
5. 把重启按钮放得太显眼。
6. 隐藏剩余流量、到期日、月租、线路、状态等关键字段。

状态视觉要求：

1. 离线服务器整行变暗。
2. 流量超过 80% 显黄。
3. 流量超过 95% 显红。
4. 到期 7 天内显黄。
5. 到期 3 天内显红。

---

## 12. 交付给 13-OTC 审查时必须提供

交付时给出：

1. 项目根目录路径。
2. 本次完成的模块列表。
3. 本地访问地址。
4. Go API 地址。
5. SQLite 数据文件位置。
6. 端口占用说明。
7. 已知未完成项。

不提供这些，13-OTC 直接拒审。

---

## 13. 最终裁决

ServerSmith 的架构主线是：

**本地 Docker 开发沙盒 → Go 管理端 + SQLite → Next.js/Tailwind/shadcn 前端 → Go embed 单二进制生产交付 → 独立静态探针 Agent。**

谁把它写成依赖堆、容器堆、企业后台模板，直接打回。

毫无美感的东西，不准进入下一阶段。

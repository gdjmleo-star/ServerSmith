# ServerSmith P3 架构蓝图（13-OTC 签发）

**签发人**：穆凝雪
**时间**：2026-05-03 17:38 CST
**基于**：P2 V2.0 已完成 ✅ → P3

---

## 1. P3 目标

1. **服务器编辑页接入告警配置**
2. **服务器详情页（只读面板）**
3. **批量操作（多选 → 批量重启）**
4. **探针管理（展示哪些服务器安装了探针、版本、状态）**

---

## 2. 接入告警配置

### 改动

在 `web/src/app/servers/[id]/edit/page.tsx` 增加一行：

```
<button onClick={openAlertConfig}>告警配置</button>
```

弹出 `AlertConfigForm` 对话框。

**改动量**：编辑页增加约 15 行，文件保持在 100 行以内。

---

## 3. 服务器详情页

### 路由

`/servers/[id]` — 只读面板，展示单台服务器的全量状态。

### 页面内容

- 标题区：服务器名称、状态 Badge、IP
- 基础信息区：类型、线路、月租、SSH 端口、用户
- 流量区（仅流量节点）：总额度、已用、剩余、百分比进度条
- 到期区（仅项目服务器）：到期日期、剩余天数
- 实时指标区：CPU、内存、磁盘最近值
- 重启记录：最近 5 条 reboot_tasks 记录
- 操作区：编辑按钮 → 跳转编辑页；重启按钮 → 触发手动重启

### 后端

`GET /api/servers/:id` 已经返回全量数据（P0+P1 已实现），详情页不需要新后端接口。

但需要加一个获取重启记录的接口：

**`GET /api/servers/:id/reboot-tasks?limit=5`**

```json
[
  { "action": "cycle_reboot", "triggered_at": "2026-05-05T02:00:00Z", "result": "success", "error_msg": null },
  ...
]
```

### 文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| web/src/app/servers/[id]/page.tsx | 新建 | 详情页，<300 行 |
| web/src/components/ServerDetail.tsx | 新建 | 详情面板组件，<300 行 |
| server/api/reboot_tasks.go | 新建 | 重启记录查询，<50 行 |
| server/main.go | 修改 | 注册新路由 |

---

## 4. 批量操作

### 多选模式

`ServerList.tsx` 增加每行左侧的 checkbox。

选中 1 台以上时，表头出现操作栏：

```
[全选] — 已选 3 台 — [批量重启] — [取消选择]
```

### 批量重启

`POST /api/servers/batch-reboot`

```json
{ "server_ids": [1, 3, 5] }
```

后端逐个执行 SSH reboot，每个服务器有单独的上下文和超时（10s），一台失败不阻塞后续。

返回结果：

```json
[
  { "server_id": 1, "result": "success" },
  { "server_id": 3, "result": "failed", "error": "timeout" },
  { "server_id": 5, "result": "success" }
]
```

### 文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| web/src/components/ServerList.tsx | 修改 | 增加 checkbox + 批量操作栏 |
| server/api/servers.go | 修改 | 增加批重启处理 |
| server/api/sql_queries.go | 不修改 | 无需新 SQL |

---

## 5. 探针管理

### 路由

`/probes` — 探针管理页面

### 页面内容

表格展示所有服务器及其探针状态：

| 列 | 内容 | 说明 |
|----|------|------|
| 服务器 | 名称 | 链接到详情页 |
| IP | host | — |
| 探针状态 | 已安装 / 未安装 / 未知 | Badge |
| 探针版本 | agent version | 从上报数据的 header 获取 |
| 最后上报 | last_report_at | 北京时间 |
| CPU | 最近值 | 小号 |
| 内存 | 最近值 | 小号 |
| 操作 | 跳转详情 | 按钮 |

### 后端变更

无需新接口。`GET /api/servers` 已返回 `last_report_at` + `status`，前端据此判断探针状态（last_report_at < 5 分钟前 = 在线，否则 = 离线，无上报记录 = 未安装）。

### 探针版本上报（可选增强）

探针上报时在请求 header 加 `X-Agent-Version`。

后端在 `servers` 表加 `agent_version` 字段（ALTER TABLE + migration），上报时更新。

### 文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| web/src/app/probes/page.tsx | 新建 | 探针列表，<30 行 |
| web/src/components/ProbeTable.tsx | 新建 | 探针状态表格，<200 行 |
| server/db/sqlite.go | 修改 | 加 agent_version migration |

---

## 6. 后端本次全量变更

| 文件 | 操作 | 行数上限 |
|------|------|----------|
| `server/api/reboot_tasks.go` | 新建 | <50 |
| `server/api/servers.go` | 修改 | <300 |
| `server/db/sqlite.go` | 修改 | —（加一列） |
| `server/main.go` | 修改 | —（两行路由） |

---

## 7. 全量 P3 文件清单

| 文件 | 操作 | 行数上限 |
|------|------|----------|
| `web/src/app/servers/[id]/page.tsx` | 新建 | <30 |
| `web/src/components/ServerDetail.tsx` | 新建 | <300 |
| `web/src/components/ProbeTable.tsx` | 新建 | <200 |
| `web/src/app/probes/page.tsx` | 新建 | <30 |
| `web/src/components/ServerList.tsx` | 修改 | <300 |
| `web/src/app/servers/[id]/edit/page.tsx` | 修改 | <100 |

---

## 8. P3 红线

1. 详情页不编辑任何数据，纯只读展示。要改数据去编辑页。
2. 批量重启不影响中间件和路由主流程。
3. 探针管理没有探针版本号时不报错，显示"未知"。
4. 批量重启的失败结果在前端用 toast 展示汇总："3 台成功，1 台失败"。

---

## 9. P3 交付检查清单

| 项 | 验收标准 | 完成 |
|---|---------|------|
| 编辑页接入告警配置 | AlertConfigForm 弹出 | ☐ |
| 服务器详情页 | 只读展示全量信息 | ☐ |
| 重启记录 | 最近 5 条，成功/失败标记 | ☐ |
| 批量多选 | checkbox + 已选计数 | ☐ |
| 批量重启 | 逐个执行，互不阻塞 | ☐ |
| 探针管理页 | 在线/离线/未安装判定 | ☐ |
| 探针版本 | last_report_at 判定安装状态 | ☐ |
| 文件红线 | 全部达标 | ☐ |

---

**架构版本**：V3.0
**签发人**：穆凝雪
**签发时间**：2026-05-03 17:38 CST

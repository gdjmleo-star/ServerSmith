# ServerSmith P5 架构蓝图（单二进制 + 生产加固）

**签发人**：穆凝雪
**时间**：2026-05-03 18:32 CST
**基于**：P4 验收 ✅（Go embed 部分未完成）

---

## 1. P5 目标

P4 的 Go embed 方案被否决，原因是三个动态页面（`/servers/[id]` 系列）都是 `"use client"` 组件，而 `output: 'export'` 只允许服务端组件使用 `generateStaticParams`。

**P5 的核心任务**：重构这三个页面，让静态导出生效。

---

## 2. 重构方案

### 困境

三个动态页面全部使用客户端渲染：
- `servers/[id]/page.tsx` — 用 `useServers` hook 发请求拿数据
- `servers/[id]/edit/page.tsx` — 同上 + 表单状态
- `servers/[id]/history/page.tsx` — 用 `useHistory` hook

静态导出需要这些页面在构建时就能生成。

### 方案：双重编译

**第一层：服务端 Layout 组件**

在 `servers/layout.tsx` 中：

```tsx
import { fetchServers } from "@/lib/api";

export async function generateStaticParams() {
  const servers = await fetchServers(); // 构建时调 API
  return servers.map((s) => ({ id: String(s.id) }));
}

export default function ServersLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
```

**但要解决循环依赖**：构建时需要 API 在线。

**方案**：不接受外部依赖，改为预构建时写入 JSON 数据文件：

```bash
# 构建前置步骤
# 1. 启动 Go API（docker compose run --rm -d server）
# 2. curl /api/servers > web/src/lib/servers-data.json
# 3. npm run build
# 4. 关闭 Go API
```

### 实际执行步骤

1. 在 `web/src/lib/` 创建 `generate-params.ts` — 无运行时依赖的构建脚本
2. `servers/layout.tsx` 引用 `generateStaticParams` 返回所有 ID
3. 编辑页和详情页保持 `"use client"`，数据走客户端请求
4. `servers/[id]/history/page.tsx` 同理

### 修改范围最小化

不改三个 `"use client"` page.tsx 的渲染逻辑。只加一层 layout 提供 `generateStaticParams`。

### 升级路线

| 步骤 | 内容 | 影响范围 |
|------|------|----------|
| 1 | 新建 `web/src/app/servers/layout.tsx` | 打通静态导出 |
| 2 | 新建 `web/src/lib/generate-params.ts` | 构建时生成 JSON |
| 3 | `next.config.ts` 加回 `output: 'export'` | 恢复静态导出 |
| 4 | `server/web.go` 重建 | Go embed 内嵌 |
| 5 | `Makefile` 更新 | 编译时先起 API 生成参数 |

---

## 3. 生产加固（同 P5 交付）

### A. 到期提醒修复

当前 `expire_notify_days` 字段已写入 DB，但 cron 到期检查使用的是硬编码 7 天。

`server/cron/expiry.go` 读取 `server_plans.expire_notify_days`。

### B. 编辑 plan_type 时清理关联数据

当服务器从 `node` 改为 `no_limit` 时，清理 `server_plans` 中残留的 cycle 相关字段。

### C. SQLite 热备份

`POST /api/backup` — 执行 `VACUUM INTO` 生成备份文件。

---

## 5. P5 全量文件变更

| 文件 | 操作 | 说明 |
|------|------|------|
| `web/src/app/servers/layout.tsx` | 新建 | layout 层生成静态参数 |
| `web/src/lib/generate-params.ts` | 新建 | 构建时生成 ID 列表 |
| `web/next.config.ts` | 修改 | 加回 `output: 'export'` |
| `server/web.go` | 重建 | Go embed 静态文件路由 |
| `server/main.go` | 修改 | 注册 embed handler |
| `Makefile` | 修改 | 两阶段构建 |
| `docker-compose.yml` | 不改 | dev 保持两服务，生产用 Makefile |
| `server/cron/expiry.go` | 修改 | 读 expire_notify_days |
| `server/api/servers.go` | 修改 | plan_type 变更时清理 |
| `server/api/backup.go` | 新建 | SQLite 备份 API |
| `server/main.go` | 修改 | 注册备份路由 |

---

## 6. P5 红线

1. `generateStaticParams` 必须在 layout 层，不透传 `"use client"` 页面。
2. 构建时的 API 调用必须超时容忍——API 不可达时返回空数组，不允许阻塞构建。
3. Go embed 必须 fallback 到 index.html（SPA 路由）。
4. 到期提醒修复必须读库，不能硬编码。

---

## 7. P5 交付检查清单

| 项 | 验收标准 | 完成 |
|---|---------|------|
| layout 层 `generateStaticParams` | 构建时生成所有服务器 ID | ☐ |
| `output: 'export'` 恢复 | `npm run build` 成功，无动态路由错误 | ☐ |
| Go embed 静态文件服务 | 单二进制跑起来能访问所有页面 | ☐ |
| SPA fallback | `/servers/999` 等动态路由显示页面不 404 | ☐ |
| 到期提醒 | cron 读库里的 `expire_notify_days`，不是 7 | ☐ |
| plan_type 变更清理 | 切流量节点↔项目机时关联数据正确 | ☐ |
| SQLite 备份 | `POST /api/backup` 生成备份文件 | ☐ |

---

**架构版本**：V5.0
**签发人**：穆凝雪
**签发时间**：2026-05-03 18:32 CST
